{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.1",
    "parameters": {
      "moduleCreateStorageAccounts": {
        "type": "object"
      },
      "customTages": {
        "type": "object"
      }
    },
    "variables": {
        "defaultLocation": "[resourceGroup().location]"
    },
    "resources": [
        {
            "copy" : {
                "name": "blobcontainer",
                "count": "[length(parameters('moduleCreateStorageAccounts').accounts)]"
            },
                    
            "name": "[concat( toLower(parameters('moduleCreateStorageAccounts').accounts[copyIndex('blobcontainer')].saName),'/default/', parameters('moduleCreateStorageAccounts').accounts[copyIndex('blobcontainer')].containerName)]",
            "type": "Microsoft.Storage/storageAccounts/blobServices",
            "apiVersion": "2018-07-01",
            "condition": "[equals(parameters('moduleCreateStorageAccounts').accounts[copyIndex('blobcontainer')].blob, 'Yes')]",
            "dependsOn": [
                "[toLower(parameters('moduleCreateStorageAccounts').accounts[copyIndex('blobcontainer')].saName)]",
                "storageAccounts"
            ],
            "properties": {
                "publicAccess": "[parameters('moduleCreateStorageAccounts').accounts[copyIndex('blobcontainer')].publicAccess]"
                
            }
                
        }      
      ], 
    "outputs": {
    }
}

  